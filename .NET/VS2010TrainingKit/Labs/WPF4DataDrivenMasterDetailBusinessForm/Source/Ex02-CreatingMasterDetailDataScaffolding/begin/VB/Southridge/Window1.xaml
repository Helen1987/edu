<Window x:Class="Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Window1" Height="600" Width="800" xmlns:my="clr-namespace:Southridge">
    <Window.Resources>
        <my:SouthridgeDataSet x:Key="SouthridgeDataSet" />
        <CollectionViewSource x:Key="ListingsViewSource" Source="{Binding Path=Listings, Source={StaticResource SouthridgeDataSet}}" />
    </Window.Resources>
    <Grid DataContext="{StaticResource ListingsViewSource}">
        <Grid DataContext="{StaticResource ListingsViewSource}" HorizontalAlignment="Left" Margin="17,30,0,0" Name="Grid1" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label Content="MLS:" Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" Margin="3" VerticalAlignment="Center" />
            <TextBox Grid.Column="1" Grid.Row="0" Height="23" HorizontalAlignment="Left" Margin="3" Name="MLSTextBox" Text="{Binding Path=MLS}" VerticalAlignment="Center" Width="120" />
            <Label Content="Title:" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" Margin="3" VerticalAlignment="Center" />
            <TextBox Grid.Column="1" Grid.Row="1" Height="23" HorizontalAlignment="Left" Margin="3" Name="TitleTextBox" Text="{Binding Path=Title}" VerticalAlignment="Center" Width="120" />
            <Label Content="Price:" Grid.Column="0" Grid.Row="2" HorizontalAlignment="Left" Margin="3" VerticalAlignment="Center" />
            <TextBox Grid.Column="1" Grid.Row="2" Height="23" HorizontalAlignment="Left" Margin="3" Name="PriceTextBox" Text="{Binding Path=Price}" VerticalAlignment="Center" Width="120" />
        </Grid>
        <Image Height="150" HorizontalAlignment="Left" Margin="224,34,0,0" Name="Image1" Stretch="Fill" VerticalAlignment="Top" Width="200" Source="{Binding Path=PrimaryPhoto}" />
    </Grid>
</Window>
