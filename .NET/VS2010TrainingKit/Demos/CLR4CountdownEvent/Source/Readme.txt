CountdownEvent is used to wait until a set of tasks are completed in order to continue the execution of a program.

With the AddCount() method, the "signal to wait" count increases while with the "Signal" method this count is decreased. Once the count reaches 0, the program execution continues.